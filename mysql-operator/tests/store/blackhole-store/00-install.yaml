apiVersion: mysql.blaqkube.io/v1alpha1
kind: Store
metadata:
  name: store1
  namespace: default
spec:
  backend: blackhole
  bucket: bucket1
  prefix: /backup/black    
  envs:
  - name: AWS_ACCESS_KEY_ID
    valueFrom:
      secretKeyRef:
        name: store-sample
        key: AWS_ACCESS_KEY_ID
---